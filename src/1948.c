#include <stdio.h>
#include  <math.h>


int del (int a, int b)
int ost (int a)
int naib (int a)




int main () {
    int n, i, s =1;
    scanf("%d", &n);
    if (n > 10000) return -1;
    if (n < 2) {
        s = 0;
    }
    else
    {
        for (i=2; i*i <= n; i++)
        {
            if (n%i == 0)
            {
                s = 0;  
                break;
            }
        }
    }
 
     return 0;




int  main() {
        int  x, d;
        d = scanf("%d", &x);
            if (d == 1) {
                  printf("%d")
             }
              else
                   printf("n/a\n");
    return 0;
    }

int ost (int a) {
	for (int = 2; i < a; i++) {
	if(del(a, i) == 0) {
		if(prost(i)==2)


 {
    int res = 0, x = a;
    while (x >= b) {
	res ++;
	x -=b;
	}
	returs x;
	}


